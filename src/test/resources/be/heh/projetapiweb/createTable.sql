CREATE TABLE IF NOT EXISTS personnes  (
                            idPerson SERIAL,
                            first_Name varchar(15),
                            last_Name varchar(15),
                            age int
);